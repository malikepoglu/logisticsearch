# EN: Controlled tracked dependency surface for the first real Python-side
# EN: LogisticSearch webcrawler worker surface.
# EN: Keep this file intentionally narrow until the repository truth proves
# EN: broader runtime layers.
#
# TR: İlk gerçek Python-tarafı LogisticSearch webcrawler worker yüzeyi için
# TR: kontrollü izlenen bağımlılık yüzeyi.
# TR: Repository doğrusu daha geniş runtime katmanlarını gerçekten kanıtlayana
# TR: kadar bu dosyayı bilinçli olarak dar tut.

psycopg[binary]==3.3.3
