"""Crawler core worker runtime package.

This package contains the moved crawler_core implementation modules.

Compatibility note:
Top-level `python_live_runtime.<module>` shim files may temporarily re-export
modules from this package so existing controls and service references do not
break during the staged topology migration.
"""
