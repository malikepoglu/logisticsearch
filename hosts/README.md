# Hosts Surface
# Host Yüzeyi

Documentation hub:
- `README.md` — repository root surface
- `docs/README.md` — documentation hub

Dokümantasyon merkezi:
- `README.md` — repository kök yüzeyi
- `docs/README.md` — dokümantasyon merkezi

## Purpose
## Amaç

This directory is the canonical repository-tracked host-operations family.

This family does **not** mirror whole machines byte-for-byte.

It mirrors the repository-tracked operational truth of each host.

Bu dizin, kanonik repository-tracked host-operations ailesidir.

Bu aile makinelerin tamamını byte-for-byte aynalamaz.

Bu aile, her host'un repository-tracked operasyonel doğrusunu aynalar.

## Current scope
## Güncel kapsam

1. `hosts/makpi51crawler/` is active now.
2. `hosts/mak-UbuntuDesktop/` is intentionally present only as an empty placeholder for its later turn.

1. `hosts/makpi51crawler/` şu anda aktiftir.
2. `hosts/mak-UbuntuDesktop/` kendi sırası gelene kadar bilinçli olarak yalnız boş placeholder olarak bulunur.
